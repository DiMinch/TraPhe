<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ─── Console Appender (Development — human-readable) ─── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) — %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ─── File Appender (Structured — machine-parseable) ─── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/traphe.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/traphe.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} | %-5level | %thread | %logger{40} | %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ─── SQL Logging (reduce noise in production) ─── -->
    <logger name="org.hibernate.SQL" level="WARN" />
    <logger name="org.hibernate.type.descriptor.sql" level="WARN" />
    <logger name="org.hibernate.orm.jdbc.bind" level="WARN" />

    <!-- ─── Spring Boot internals ─── -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.springframework.web" level="INFO" />
    <logger name="org.springframework.security" level="INFO" />

    <!-- ─── Application ─── -->
    <logger name="com.example.traphe_backend" level="DEBUG" />

    <!-- ─── Root ─── -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
